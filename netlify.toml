[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "frontend/dist"

# Redirect rules for Vue Router (if using history mode in the future)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

