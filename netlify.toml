[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Redirect rules for Vue Router (if using history mode in the future)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

